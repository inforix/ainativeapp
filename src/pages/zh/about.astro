---
import BaseHead from '../../components/BaseHead.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import { getLangFromUrl, useTranslations } from '../../i18n/languages';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<!doctype html>
<html lang="zh">
	<head>
		<BaseHead title={t('about.title')} description={t('about.mission.desc')} />
	</head>
	<body>
		<Header />
		<main>
			<!-- Hero Section -->
			<section class="text-center py-16 bg-gradient-primary text-white">
				<h1 class="text-5xl md:text-6xl font-bold mb-4">{t('about.title')}</h1>
				<p class="text-xl opacity-90 max-w-2xl mx-auto px-4">{t('about.subtitle')}</p>
			</section>

			<!-- Mission -->
			<section class="py-16 bg-gray-50">
				<div class="container-custom">
					<h2 class="text-4xl font-bold text-center mb-8 text-gray-800">{t('about.mission.title')}</h2>
					<p class="text-xl leading-relaxed text-center max-w-3xl mx-auto text-gray-600">{t('about.mission.desc')}</p>
				</div>
			</section>

			<!-- What We Do -->
			<section class="py-16">
				<div class="container-custom">
					<h2 class="text-4xl font-bold text-center mb-8 text-gray-800">{t('about.what.title')}</h2>
					<div class="grid grid-cols-1 md:grid-cols-3 gap-8 mt-12">
						<div class="card text-center p-10 hover:-translate-y-1">
							<div class="text-5xl mb-6">📚</div>
							<h3 class="text-xl font-semibold text-gray-800 mb-4">{t('about.what.item1.title')}</h3>
							<p class="text-gray-600 leading-relaxed">{t('about.what.item1.desc')}</p>
						</div>
						<div class="card text-center p-10 hover:-translate-y-1">
							<div class="text-5xl mb-6">🔬</div>
							<h3 class="text-xl font-semibold text-gray-800 mb-4">{t('about.what.item2.title')}</h3>
							<p class="text-gray-600 leading-relaxed">{t('about.what.item2.desc')}</p>
						</div>
						<div class="card text-center p-10 hover:-translate-y-1">
							<div class="text-5xl mb-6">🤝</div>
							<h3 class="text-xl font-semibold text-gray-800 mb-4">{t('about.what.item3.title')}</h3>
							<p class="text-gray-600 leading-relaxed">{t('about.what.item3.desc')}</p>
						</div>
					</div>
				</div>
			</section>

			<!-- Book Section -->
			<section class="py-16 bg-gray-50">
				<div class="container-custom">
					<div class="grid grid-cols-1 lg:grid-cols-3 gap-16 items-center">
						<div class="lg:col-span-2">
							<h2 class="text-4xl font-bold mb-6 text-gray-800">{t('about.book.title')}</h2>
							<p class="text-lg leading-relaxed text-gray-600 mb-8">{t('about.book.desc')}</p>
							
							<div class="mb-8">
								<h3 class="text-xl font-semibold mb-4 text-gray-800">{t('about.book.topics.title')}</h3>
								<ul class="list-none p-0 space-y-2">
									<li class="relative pl-6 text-gray-600 before:content-['✓'] before:text-green-500 before:font-bold before:absolute before:left-0">{t('about.book.topic1')}</li>
									<li class="relative pl-6 text-gray-600 before:content-['✓'] before:text-green-500 before:font-bold before:absolute before:left-0">{t('about.book.topic2')}</li>
									<li class="relative pl-6 text-gray-600 before:content-['✓'] before:text-green-500 before:font-bold before:absolute before:left-0">{t('about.book.topic3')}</li>
									<li class="relative pl-6 text-gray-600 before:content-['✓'] before:text-green-500 before:font-bold before:absolute before:left-0">{t('about.book.topic4')}</li>
									<li class="relative pl-6 text-gray-600 before:content-['✓'] before:text-green-500 before:font-bold before:absolute before:left-0">{t('about.book.topic5')}</li>
									<li class="relative pl-6 text-gray-600 before:content-['✓'] before:text-green-500 before:font-bold before:absolute before:left-0">{t('about.book.topic6')}</li>
								</ul>
							</div>
							
							<div class="p-4 bg-teal-50 border-l-4 border-teal-500 rounded text-gray-800">
								<strong>{t('about.book.release')}</strong>
							</div>
						</div>
						
						<div class="flex justify-center">
							<div class="relative w-50 h-70 transform -rotate-12 hover:rotate-0 transition-transform duration-300 shadow-book">
								<div class="absolute -left-2 top-0 w-2 h-full bg-gradient-to-b from-gray-600 to-gray-800 transform rotate-y-90"></div>
								<div class="w-full h-full bg-gradient-primary rounded-lg flex flex-col justify-between p-8 text-white">
									<div class="text-2xl font-bold leading-tight text-center">智能原生<br/>应用</div>
									<div class="text-sm opacity-80 text-center">完整指南</div>
									<div class="text-3xl text-center">🤖</div>
								</div>
							</div>
						</div>
					</div>
				</div>
			</section>

			<!-- Vision -->
			<section class="py-16 text-center">
				<div class="container-custom">
					<h2 class="text-4xl font-bold mb-8 text-gray-800">{t('about.vision.title')}</h2>
					<p class="text-xl leading-relaxed max-w-3xl mx-auto text-gray-600">{t('about.vision.desc')}</p>
				</div>
			</section>

			<!-- Expertise -->
			<section class="py-16 bg-gray-50 text-center">
				<div class="container-custom">
					<h2 class="text-4xl font-bold mb-8 text-gray-800">{t('about.team.title')}</h2>
					<p class="text-lg leading-relaxed max-w-2xl mx-auto text-gray-600">{t('about.team.desc')}</p>
				</div>
			</section>

			<!-- CTA Section -->
			<section class="py-16 bg-gradient-primary text-white text-center">
				<div class="container-custom">
					<h2 class="text-4xl font-bold mb-4 text-white">{t('about.contact.title')}</h2>
					<p class="text-xl mb-8 opacity-90 max-w-2xl mx-auto">{t('about.contact.desc')}</p>
					<div class="flex flex-col sm:flex-row gap-4 justify-center items-center flex-wrap">
						<a href="/zh/how" class="btn-primary">了解如何构建</a>
						<a href="/zh/cases" class="btn-secondary">探索成功案例</a>
					</div>
				</div>
			</section>
		</main>
		<Footer />
		
	</body>
</html>